{% extends 'base.html.twig' %}

{% block title %}Hello TeamController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
   
    {% if teams is not empty and teams is defined %}
    {% for team in teams %}
{{ team.name~' '~ team.description   }}

<tr>
<td>
<a href ="{{path('app_editteam',{'id':team.id})}}">Edit</a> 
<a href ="{{path('app_deleteteam',{'id':team.id})}}">Delete</a>        
</td>
</tr>
<br>

{% endfor %}
<a href ="{{path('team_add')}}">Add</a>
{%   else %}
Le tableau est vide <br>
AJOUTER DES TEAMS <br>
<a href ="{{path('team_add')}}">Add</a>
{% endif %}
    
</div>
{% endblock %}
